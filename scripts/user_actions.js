/*
	Each user action corresponds with a behavior the user can take.
	User actions may result in a change in the display of the page.
*/
function clickButton(btn){

	// call button depending on which id is passed thru
}

